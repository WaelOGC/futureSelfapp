# Replicate API token (required for image aging)
REPLICATE_API_TOKEN=__REPLACE_ME__

# OpenAI API key (required for wisdom letter generation)
OPENAI_API_KEY=__REPLACE_ME__

# Gemini API key (optional, for future Gemini provider support)
GEMINI_API_KEY=__REPLACE_ME__

# Anthropic API key (optional, for future Anthropic provider support)
ANTHROPIC_API_KEY=__REPLACE_ME__

# OpenAI Project ID (optional, for project-based API keys)
OPENAI_PROJECT_ID=__REPLACE_ME__
