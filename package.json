{
  "name": "future-self-app",
  "version": "1.0.0",
  "description": "Future Self AI application",
  "main": "src/dev/run-ai-task.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev:test-payload": "node dev/tools/generateTestPayload.js"
  },
  "dependencies": {
    "dotenv": "^16.6.1",
    "openai": "^6.13.0"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}
